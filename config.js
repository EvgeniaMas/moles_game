let data = {     
  time: 30000,  // time in mseconds, so 30000 is 30 seconds minutes
  x: 2500, // time in mseconds, mole is visible, so 2,5 seconds;
  y: 500    // time is also in mseconds, new mole appear from a hole;    
};